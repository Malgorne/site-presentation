import { MoveBoxDirective } from './move-box.directive';

describe('MoveBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveBoxDirective();
    expect(directive).toBeTruthy();
  });
});
