import { BolterDirective } from './bolter.directive';

describe('BolterDirective', () => {
  it('should create an instance', () => {
    const directive = new BolterDirective();
    expect(directive).toBeTruthy();
  });
});
